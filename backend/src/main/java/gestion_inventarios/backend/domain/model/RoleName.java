package gestion_inventarios.backend.domain.model;

/**
 * Nombres canónicos de roles del negocio.
 * Usados para comparación en lógica de dominio.
 */
public enum RoleName {
    ADMIN,
    EMPLOYEE,
    MANAGER
}
